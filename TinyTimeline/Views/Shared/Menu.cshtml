@model TinyTimeline.Models.MenuModel
<link rel="stylesheet" href="~/css/site.css" />

<table class="menu margin_bottom">
        <tr>
                <td>@Html.ActionLink("Current session", "Session", "Presentation",new {sessionId = Model.SessionId})</td>
                <td>@Html.ActionLink("All sessions", "Sessions", "Presentation")</td>
        </tr>
</table>
